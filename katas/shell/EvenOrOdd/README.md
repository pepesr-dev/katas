# Even or Odd
oge un int como argumento y retorna even o odd.
Recibe un número entero como argumento y retorna even (par) o odd (impar).

## Ejemplos
- `./script.sh 0` (Even)
- `./script.sh 1` (Odd)

## Instalación y uso
- Descarga el .sh
- Dar permiso de ejecución
- Ejecutar 
